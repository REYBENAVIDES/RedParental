<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@drawable/img_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/img_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RedPaternal.NoActionBar"
        tools:targetApi="34">
        <service
            android:name=".servicios.MonitoreoService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <activity
            android:name=".presentacion.dashboard.router.RouterDetalleActivity"
            android:exported="false"
            android:theme="@style/Theme.RedPaternal.NoActionBar" />
        <activity
            android:name=".presentacion.router_configurar.RouterConfigurarActivity"
            android:exported="false"
            android:parentActivityName=".presentacion.onboarding.bienvenida.ActividadOnboarding" /> <!-- Navegación back stack mejorada -->
        <!-- Actividad principal de onboarding (bienvenida: LAUNCHER, se ejecuta PRIMERO) -->
        <activity
            android:name=".presentacion.onboarding.bienvenida.ActividadOnboarding"
            android:exported="true"
            android:theme="@style/Theme.RedPaternal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Actividad principal del dashboard (post-login y config router) -->
        <activity
            android:name=".presentacion.dashboard.home.HomeActivity"
            android:exported="false"
            android:launchMode="singleTop" /> <!-- Evita múltiples instancias para mejor UX -->
        <!-- Meta-datos de Firebase y Google Services (para auth y Firestore) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>