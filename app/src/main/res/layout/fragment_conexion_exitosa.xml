<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo_1"
    android:orientation="vertical"
    android:padding="24dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Espacio superior -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Contenedor del contenido principal centrado -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Icono de éxito -->
        <ImageView
            android:id="@+id/ivSuccessIcon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_exito"
            app:tint="@color/primario" />

        <!-- Título -->
        <TextView
            android:id="@+id/tvSuccessTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="¡Router Agregado!"
            android:textColor="@color/titulo"
            android:textSize="28sp"
            android:textStyle="bold" />

        <!-- Subtítulo -->
        <TextView
            android:id="@+id/tvSuccessSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:layout_marginStart="48dp"
            android:layout_marginEnd="48dp"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:text="El router ha sido agregado al sistema. Ahora puedes monitorear y gestionar filtros en la red."
            android:textAlignment="center"
            android:textColor="@color/texto"
            android:textSize="16sp" />

    </LinearLayout>

    <!-- Espacio flexible -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>